"use client";
import Image from "next/image";
import Link from "next/link";
import { useEffect, useState } from "react";
type Props={children:React.ReactNode;mode:"sign-up"|"sign-in"};
const stats=[["Developers joining today","1,248"],["Discussions this week","8,420"],["Articles published","2,186"],["Research papers","742"],["AI summaries generated","18.6k"]];
export function AuthShell({children,mode}:Props){const [index,setIndex]=useState(0);useEffect(()=>{const timer=window.setInterval(()=>setIndex(value=>(value+1)%stats.length),4200);return()=>window.clearInterval(timer)},[]);return <main className="auth-page"><section className="auth-hero" aria-labelledby="auth-hero-title"><div className="auth-hero-glow" aria-hidden="true"/><div className="auth-graph" aria-hidden="true"><span/><span/><span/><span/><i/><i/><i/></div><Link href="/" className="auth-brand" aria-label="Pnyx home"><Image src="/brand/pnyx-mark.svg" alt="" width={52} height={52} priority/><span>PNYX</span></Link><div className="auth-hero-content"><p className="auth-kicker">A technology knowledge network</p><h1 id="auth-hero-title">Join Pnyx</h1><p className="auth-tagline">Where technology conversations become organised knowledge.</p><p className="auth-copy">Build your technology identity. Share ideas. Learn from experts. Publish knowledge. Discover innovations. Everything you contribute becomes part of a living technology network.</p><div className="auth-stats" aria-live="polite"><strong>{stats[index][1]}</strong><span>{stats[index][0]}</span><div className="auth-stat-dots" aria-hidden="true">{stats.map((_,dot)=><i className={dot===index?"active":""} key={dot}/>)}</div></div></div><div className="auth-particles" aria-hidden="true">{Array.from({length:12},(_,particle)=><i key={particle}/>)}</div></section><section className="auth-panel"><div className="auth-panel-inner"><div className="auth-mobile-brand"><Link href="/" className="auth-brand auth-brand-light" aria-label="Pnyx home"><Image src="/brand/pnyx-mark.svg" alt="" width={38} height={38}/><span>PNYX</span></Link></div>{children}<p className="auth-switch">{mode==="sign-up"?<>Already part of the commons? <Link href="/sign-in">Sign in</Link></>:<>New to Pnyx? <Link href="/sign-up">Create an account</Link></>}</p></div></section></main>}