"use client";
import { useState } from "react";
export function KnowledgeEditor(){const [form,setForm]=useState({spaceSlug:"programming",type:"GUIDE",title:"",summary:"",body:"",status:"DRAFT",sourcePostId:""});const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);const [draftBusy,setDraftBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const r=await fetch("/api/artifacts",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const d=await r.json();setBusy(false);if(!r.ok){setMessage(d.error??"Could not save artifact.");return}setMessage("Draft saved. You can add sources from the artifact page.");setForm(f=>({...f,title:"",summary:"",body:""}))}async function assist(){if(!form.sourcePostId){setMessage("Add the public discussion ID first.");return}setDraftBusy(true);const r=await fetch(`/api/ai/draft`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({postId:form.sourcePostId})});const d=await r.json();setDraftBusy(false);if(!r.ok){setMessage(d.error??"Could not draft from this discussion.");return}setForm(f=>({...f,body:d.text??f.body,sourcePostId:form.sourcePostId}));setMessage("AI draft inserted for review. Edit it before saving or publishing.")}return <form onSubmit={submit} className="panel space-y-4" aria-labelledby="knowledge-editor-title"><div><p className="eyebrow">Contribute to the commons</p><h2 id="knowledge-editor-title" className="text-2xl font-semibold text-[var(--navy)]">Create a knowledge artifact</h2><p className="help mt-2">Draft a guide, answer, research note, benchmark, or case study. AI assistance only uses a selected public discussion and never publishes automatically.</p></div><div className="grid gap-3 sm:grid-cols-2"><label>Space<input required value={form.spaceSlug} onChange={e=>setForm({...form,spaceSlug:e.target.value})} /></label><label>Type<select value={form.type} onChange={e=>setForm({...form,type:e.target.value})}>{["GUIDE","ANSWER","RESEARCH","BENCHMARK","CASE_STUDY"].map(t=><option key={t}>{t}</option>)}</select></label></div><label>Source discussion ID (optional)<input value={form.sourcePostId} onChange={e=>setForm({...form,sourcePostId:e.target.value})} placeholder="Paste a public discussion ID" /><span className="help">Use only a discussion you are allowed to reuse. The source remains attributed.</span></label><button type="button" className="button-outline" disabled={draftBusy} onClick={assist}>{draftBusy?"Drafting…":"Assist from selected discussion"}</button><label>Title<input required minLength={3} maxLength={200} value={form.title} onChange={e=>setForm({...form,title:e.target.value})} placeholder="Name the knowledge clearly" /></label><label>Summary<textarea rows={3} maxLength={1000} value={form.summary} onChange={e=>setForm({...form,summary:e.target.value})} placeholder="What will readers learn?" /></label><label>Body<textarea required rows={12} maxLength={50000} value={form.body} onChange={e=>setForm({...form,body:e.target.value})} placeholder="Write or edit the durable explanation in Markdown." /></label><button className="button-primary" disabled={busy}>{busy?"Saving…":"Save draft"}</button>{message&&<p role="status" className="help">{message}</p>}</form>}