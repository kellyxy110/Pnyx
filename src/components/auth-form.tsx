"use client";
import { FormEvent, useEffect, useMemo, useState } from "react";
import { signIn } from "next-auth/react";
import Link from "next/link";

type FieldErrors=Record<string,string>;
function EyeButton({visible,onClick,label}:{visible:boolean;onClick:()=>void;label:string}){return <button type="button" className="auth-eye" onClick={onClick} aria-label={label} title={label}>{visible?"◉":"◌"}</button>}
function SocialButtons(){const [message,setMessage]=useState("");async function social(provider:string,label:string){setMessage("");try{const available=await fetch("/api/auth/providers").then(response=>response.json());if(!available?.[provider]){setMessage(`${label} sign-in is not configured yet.`);return}const result=await signIn(provider,{callbackUrl:"/spaces"}) as {error?:string}|undefined;if(result?.error)setMessage(`${label} sign-in could not be completed. Please try again.`)}catch{setMessage("Unexpected server error. Please try again.")}}return <><div className="social-grid"><button type="button" className="social-button" onClick={()=>social("google","Google")}><span aria-hidden="true">G</span>Continue with Google</button><button type="button" className="social-button" onClick={()=>social("github","GitHub")}><span aria-hidden="true">◈</span>Continue with GitHub</button><button type="button" className="social-button muted" onClick={()=>setMessage("Apple sign-in is coming after provider configuration.")}> <span>Apple</span></button><button type="button" className="social-button muted" onClick={()=>setMessage("Microsoft sign-in is coming after provider configuration.")}>▦ <span>Microsoft</span></button></div>{message&&<p className="auth-form-note" role="status">{message}</p>}<div className="auth-divider"><span>or continue with email</span></div></>}
function PasswordRequirements({password}:{password:string}){const checks=useMemo(()=>[{label:"At least 8 characters",ok:password.length>=8},{label:"Uppercase",ok:/[A-Z]/.test(password)},{label:"Lowercase",ok:/[a-z]/.test(password)},{label:"Number",ok:/\d/.test(password)},{label:"Special character",ok:/[^A-Za-z0-9]/.test(password)}],[password]);return <ul className="password-requirements" aria-label="Password requirements">{checks.map(check=><li className={check.ok?"met":""} key={check.label}><span aria-hidden="true">{check.ok?"✓":"○"}</span>{check.label}</li>)}</ul>}
function PasswordField({name,label,value,onChange,autoComplete,error,showRequirements=false}:{name:string;label:string;value:string;onChange:(value:string)=>void;autoComplete:string;error?:string;showRequirements?:boolean}){const [visible,setVisible]=useState(false);const [caps,setCaps]=useState(false);const strength=value.length<8?"Weak":value.length<12?"Fair":/[A-Z]/.test(value)&&/[a-z]/.test(value)&&/\d/.test(value)&&/[^A-Za-z0-9]/.test(value)?"Excellent":"Strong";return <div className="auth-field"><label htmlFor={name}>{label}</label><div className="auth-input-wrap"><input id={name} name={name} type={visible?"text":"password"} value={value} onChange={event=>onChange(event.target.value)} onKeyDown={event=>setCaps(event.getModifierState("CapsLock"))} onKeyUp={event=>setCaps(event.getModifierState("CapsLock"))} autoComplete={autoComplete} aria-invalid={Boolean(error)} aria-describedby={`${name}-help ${name}-error`} required/><EyeButton visible={visible} onClick={()=>setVisible(!visible)} label={visible?`Hide ${label.toLowerCase()}`:`Reveal ${label.toLowerCase()}`}/></div>{caps&&<p id={`${name}-help`} className="caps-indicator" role="status">Caps Lock is on</p>}{error&&<p id={`${name}-error`} className="auth-error" role="alert">{error}</p>}{showRequirements&&<><div className="strength-row"><span>Password strength</span><strong className={`strength-${strength.toLowerCase()}`}>{strength}</strong></div><div className="strength-meter" aria-hidden="true"><i className={`strength-fill strength-fill-${strength.toLowerCase()}`}/></div><PasswordRequirements password={value}/><p className="auth-hint">Use 12 or more characters for your Pnyx account.</p></>}</div>}
function UsernameField({value,onChange,error}:{value:string;onChange:(value:string)=>void;error?:string}){const [availability,setAvailability]=useState<"idle"|"checking"|"available"|"taken"|"invalid">("idle");useEffect(()=>{if(value.length<3||!/^[a-z0-9_-]+$/.test(value)){setAvailability(value?"invalid":"idle");return}setAvailability("checking");const controller=new AbortController();const timer=window.setTimeout(async()=>{try{const response=await fetch(`/api/account/username?username=${encodeURIComponent(value)}`,{signal:controller.signal});const data=await response.json();setAvailability(data.available?"available":"taken")}catch(error){if((error as Error).name!=="AbortError")setAvailability("idle")}},350);return()=>{controller.abort();window.clearTimeout(timer)}},[value]);const message=availability==="checking"?"Checking availability…":availability==="available"?"✓ Available":availability==="taken"?"Username already taken":availability==="invalid"?"Use lowercase letters, numbers, - or _":"Your public profile: pnyx.com/@kellyxy";return <div className="auth-field"><label htmlFor="username">Username</label><div className="auth-input-wrap"><span className="input-prefix" aria-hidden="true">@</span><input id="username" name="username" value={value} onChange={event=>onChange(event.target.value.toLowerCase())} autoComplete="username" placeholder="kellyxy" minLength={3} maxLength={30} aria-invalid={Boolean(error)||availability==="taken"} aria-describedby="username-help username-error" required/></div><p id="username-help" className={`username-status username-${availability}`} role="status">{message}</p>{error&&<p id="username-error" className="auth-error" role="alert">{error}</p>}</div>}
export function SignUpForm(){const [form,setForm]=useState({displayName:"",username:"",email:"",password:"",confirmPassword:""});const [errors,setErrors]=useState<FieldErrors>({});const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);function update(key:keyof typeof form,value:string){setForm(current=>({...current,[key]:value}));setErrors(current=>({...current,[key]:""}));setMessage("")}function validate(){const next:FieldErrors={};if(!form.displayName.trim())next.displayName="Display name is required.";else if(form.displayName.trim().length<2)next.displayName="Display name must be at least 2 characters.";if(!form.username)next.username="Username is required.";else if(!/^[a-z0-9_-]+$/.test(form.username))next.username="Username must contain only lowercase letters, numbers, - or _.";if(!form.email)next.email="Email address is required.";else if(!/^\S+@\S+\.\S+$/.test(form.email))next.email="Email address is invalid.";if(form.password.length<8)next.password="Password must be at least 8 characters.";if(form.password.length<12)next.password="Use 12 or more characters for your Pnyx account.";if(form.confirmPassword!==form.password)next.confirmPassword="Passwords do not match.";return next}async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const next=validate();if(Object.keys(next).length){setErrors(next);setMessage("Please correct the highlighted fields.");return}setBusy(true);setErrors({});setMessage("");try{const response=await fetch("/api/account/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({displayName:form.displayName,username:form.username,email:form.email,password:form.password})});const result=await response.json();if(!response.ok){setErrors(result.fieldErrors?Object.fromEntries(Object.entries(result.fieldErrors).map(([key,value])=>[key,Array.isArray(value)?value[0]:String(value)])):{});setMessage(result.error??"Unexpected server error. Please try again.");return}setMessage(result.message);setForm({displayName:"",username:"",email:"",password:"",confirmPassword:""})}catch{setMessage("Unexpected server error. Please try again.")}finally{setBusy(false)}}return <form onSubmit={submit} className="auth-form" noValidate aria-describedby="signup-description"><header className="auth-form-header"><p className="auth-kicker">Join Pnyx</p><h1>Create your account</h1><p id="signup-description">Create a technology identity built around what you learn, build, research and share.</p></header><SocialButtons/><div className="auth-field"><label htmlFor="displayName">Display Name</label><input id="displayName" name="displayName" value={form.displayName} onChange={e=>update("displayName",e.target.value)} autoComplete="name" aria-invalid={Boolean(errors.displayName)} aria-describedby="displayName-error" required/>{errors.displayName&&<p id="displayName-error" className="auth-error" role="alert">{errors.displayName}</p>}</div><UsernameField value={form.username} onChange={value=>update("username",value)} error={errors.username}/><div className="auth-field"><label htmlFor="email">Email</label><input id="email" name="email" type="email" value={form.email} onChange={e=>update("email",e.target.value)} autoComplete="email" aria-invalid={Boolean(errors.email)} aria-describedby="email-error" required/>{errors.email&&<p id="email-error" className="auth-error" role="alert">{errors.email}</p>}</div><PasswordField name="password" label="Password" value={form.password} onChange={value=>update("password",value)} autoComplete="new-password" error={errors.password} showRequirements/><PasswordField name="confirmPassword" label="Confirm Password" value={form.confirmPassword} onChange={value=>update("confirmPassword",value)} autoComplete="new-password" error={errors.confirmPassword}/>{message&&<p className={message.startsWith("Account")?"auth-success":"auth-form-note"} role={message.startsWith("Account")?"status":"alert"}>{message}</p>}<button className="auth-submit" disabled={busy}>{busy?<><span className="auth-spinner" aria-hidden="true"/>Creating your account...</>:"Create my Pnyx account"}</button><p className="auth-legal">By creating an account, you agree to participate respectfully in the Pnyx commons.</p></form>}
export function SignInForm(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();if(!email||!/^\S+@\S+\.\S+$/.test(email)){setError("Email address is invalid.");return}if(!password){setError("Password is required.");return}setBusy(true);setError("");try{const result=await signIn("credentials",{email,password,redirect:false});if(result?.error)setError("Email or password is incorrect, or your email has not been verified.");else window.location.assign("/spaces")}catch{setError("Unexpected server error. Please try again.")}finally{setBusy(false)}}return <form onSubmit={submit} className="auth-form" noValidate aria-describedby="signin-description"><header className="auth-form-header"><p className="auth-kicker">Welcome back</p><h1>Welcome back</h1><p id="signin-description">Continue building your technology identity.</p></header><SocialButtons/><div className="auth-field"><label htmlFor="signin-email">Email</label><input id="signin-email" name="email" type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email" aria-invalid={Boolean(error&&(!email||!/^\S+@\S+\.\S+$/.test(email)))} required/></div><PasswordField name="signin-password" label="Password" value={password} onChange={setPassword} autoComplete="current-password" error={error&&password?undefined:error}/><div className="auth-forgot"><Link href="/forgot-password">Forgot your password?</Link></div>{error&&<p className="auth-error" role="alert">{error}</p>}<button className="auth-submit" disabled={busy}>{busy?<><span className="auth-spinner" aria-hidden="true"/>Signing in...</>:"Sign in to Pnyx"}</button></form>}