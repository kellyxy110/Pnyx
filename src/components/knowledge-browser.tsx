"use client";
import { useEffect, useState } from "react";
type Artifact={id:string;title:string;summary:string|null;type:string;status:string;updatedAt:string;author:{displayName:string;username:string};space:{name:string;slug:string};_count:{revisions:number;sources:number;contributors:number}};
export function KnowledgeBrowser(){const [items,setItems]=useState<Artifact[]>([]);const [state,setState]=useState("Loading knowledge…");useEffect(()=>{fetch("/api/artifacts").then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error);setItems(d.artifacts??[]);setState(d.artifacts?.length?"":"No published knowledge yet.")}).catch(e=>setState(e.message??"Knowledge could not be loaded."))},[]);return <section className="space-y-5" aria-labelledby="knowledge-list-title"><div><p className="eyebrow">Organised knowledge</p><h2 id="knowledge-list-title" className="text-3xl font-semibold text-[var(--navy)]">Guides, answers, and research</h2></div>{items.length?items.map(a=><article key={a.id} className="panel"><div className="flex flex-wrap justify-between gap-2"><span className="badge">{a.type.toLowerCase()} · {a.status.toLowerCase().replace("_"," ")}</span><span className="text-sm text-[var(--muted)]">{a.space.name}</span></div><h3 className="mt-3 text-xl font-semibold"><a href={`/knowledge/${a.id}`} className="hover:underline">{a.title}</a></h3>{a.summary&&<p className="mt-2 text-[var(--muted)]">{a.summary}</p>}<p className="mt-3 text-sm text-[var(--muted)]">By {a.author.displayName} · {a._count.revisions} revision{a._count.revisions===1?"":"s"} · {a._count.sources} source{a._count.sources===1?"":"s"}</p></article>):<p className="panel" role="status">{state}</p>}</section>}
