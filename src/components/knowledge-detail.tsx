"use client";
import { useMemo, useState } from "react";
import Link from "next/link";
type Contributor={user:{displayName:string};role:string}; type Source={id:string;url:string;title:string|null;citation:string|null}; type Revision={id:string;revisionNumber:number;changeSummary:string|null;editor:{displayName:string}}; type Artifact={id:string;title:string;summary:string|null;body:string;type:string;status:string;author:{displayName:string;username:string};space:{name:string;slug:string};contributors:Contributor[];sources:Source[];revisions:Revision[]};
export function KnowledgeDetail({artifact,canEdit}:{artifact:Artifact;canEdit:boolean}){const [url,setUrl]=useState("");const [title,setTitle]=useState("");const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);const headings=useMemo(()=>artifact.body.split("\n").filter(line=>/^#{1,3}\s+/.test(line)).map(line=>line.replace(/^#+\s+/,"")),[artifact.body]); async function call(path:string,method:string,payload?:unknown){const r=await fetch(path,{method,headers:payload?{"Content-Type":"application/json"}:undefined,body:payload?JSON.stringify(payload):undefined});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error??"This action could not be completed.");return d} async function addSource(e:React.FormEvent){e.preventDefault();setBusy(true);try{await call(`/api/artifacts/${artifact.id}/sources`,"POST",{url,title});setMessage("Source added. Refresh to see it in the article.");setUrl("");setTitle("")}catch(e){setMessage(e instanceof Error?e.message:"Could not add source.")}finally{setBusy(false)}}async function removeSource(sourceId:string){setBusy(true);try{await call(`/api/artifacts/${artifact.id}/sources?sourceId=${sourceId}`,"DELETE");setMessage("Source removed. Refresh to update the article.")}catch(e){setMessage(e instanceof Error?e.message:"Could not remove source.")}finally{setBusy(false)}}async function report(e:React.FormEvent){e.preventDefault();const data=new FormData(e.currentTarget as HTMLFormElement);setBusy(true);try{await call(`/api/artifacts/${artifact.id}/report`,"POST",{reason:data.get("reason"),details:data.get("details")});setMessage("Correction report submitted.")}catch(e){setMessage(e instanceof Error?e.message:"Could not submit correction.")}finally{setBusy(false)}}async function share(){try{await navigator.clipboard.writeText(location.href);setMessage("Knowledge link copied.")}catch{setMessage("Copy the address from your browser to share this article.")}}return <main className="knowledge-reading"><div className="knowledge-main"><Link href="/knowledge" className="text-link">← Knowledge library</Link><article className="knowledge-article"><header><p className="eyebrow">{artifact.space.name} · {artifact.type.toLowerCase()}</p><span className="badge">{artifact.status.toLowerCase().replace("_"," ")}</span><h1>{artifact.title}</h1>{artifact.summary&&<p className="knowledge-summary">{artifact.summary}</p>}<p className="knowledge-byline">Written by <Link href={`/people/${artifact.author.username}`}>{artifact.author.displayName}</Link> · {artifact.revisions.length} revision{artifact.revisions.length===1?"":"s"}</p><div className="knowledge-actions"><button className="button-outline" onClick={share}>Share</button><Link className="button-outline" href={`/spaces/${artifact.space.slug}`}>View Space</Link></div></header><div className="knowledge-body">{artifact.body.split("\n\n").map((paragraph,index)=>paragraph.startsWith("### ")?<h3 key={index}>{paragraph.slice(4)}</h3>:paragraph.startsWith("## ")?<h2 key={index}>{paragraph.slice(3)}</h2>:paragraph.startsWith("# ")?<h2 key={index}>{paragraph.slice(2)}</h2>:<p key={index}>{paragraph}</p>)}</div><footer className="knowledge-contributors"><strong>Contributors</strong><span>{artifact.contributors.length?artifact.contributors.map(c=>`${c.user.displayName} (${c.role.toLowerCase()})`).join(", "):"Original author"}</span></footer></article></div><aside className="knowledge-rail"><section className="panel"><h2>In this article</h2>{headings.length?<ol>{headings.map((heading,index)=><li key={`${heading}-${index}`}>{heading}</li>)}</ol>:<p className="help">Read the full article below.</p>}</section><section className="panel"><h2>Sources and citations</h2>{artifact.sources.length?<ol className="knowledge-sources">{artifact.sources.map(s=><li key={s.id}><a href={s.url} target="_blank" rel="noreferrer">{s.title||s.url}</a>{s.citation&&<p>{s.citation}</p>}{canEdit&&<button className="text-link" disabled={busy} onClick={()=>void removeSource(s.id)}>Remove source</button>}</li>)}</ol>:<p className="help">No sources added yet.</p>}{canEdit&&<form onSubmit={addSource} className="knowledge-form"><label>Source URL<input required type="url" value={url} onChange={e=>setUrl(e.target.value)}/></label><label>Source title<input value={title} onChange={e=>setTitle(e.target.value)}/></label><button className="button-outline" disabled={busy}>Add source</button></form>}</section><section className="panel"><h2>Suggest a correction</h2><form onSubmit={report} className="knowledge-form"><label>Reason<select name="reason"><option value="INCORRECT">Incorrect</option><option value="OUTDATED">Outdated</option><option value="UNSOURCED">Needs sources</option><option value="COPYRIGHT">Copyright concern</option><option value="OTHER">Other</option></select></label><label>Details<textarea name="details" rows={4} placeholder="Explain what should be reviewed."/></label><button className="button-outline" disabled={busy}>Submit correction</button></form></section><section className="panel"><h2>Revision history</h2><ol className="knowledge-revisions">{artifact.revisions.map(r=><li key={r.id}><strong>Revision {r.revisionNumber}</strong><span>{r.changeSummary||"Updated"} · {r.editor.displayName}</span></li>)}</ol></section></aside>{message&&<p role="status" className="knowledge-message">{message}</p>}</main>}
