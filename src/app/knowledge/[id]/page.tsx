import { notFound } from "next/navigation"; import { auth } from "@/auth"; import { prisma } from "@/lib/prisma"; import { canEditArtifact } from "@/lib/knowledge"; import { KnowledgeDetail } from "@/components/knowledge-detail"; import { ProductNav } from "@/components/product-nav";
export default async function KnowledgeArtifactPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const artifact=await prisma.artifact.findUnique({where:{id},include:{author:{select:{displayName:true,username:true}},space:{select:{name:true,slug:true}},contributors:{include:{user:{select:{displayName:true,username:true}}}},sources:{orderBy:{addedAt:"asc"}},revisions:{orderBy:{revisionNumber:"desc"},include:{editor:{select:{displayName:true,username:true}}}}}});if(!artifact)notFound();if(artifact.status==="DRAFT"&&!(await canEditArtifact((await auth())?.user?.id??"",id)))notFound();return <><ProductNav/><KnowledgeDetail artifact={artifact}/></>}
