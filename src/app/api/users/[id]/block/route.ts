import { NextResponse } from "next/server"; import { auth } from "@/auth"; import { prisma } from "@/lib/prisma";
export async function POST(_r:Request,c:{params:Promise<{id:string}>}){const s=await auth();if(!s?.user?.id)return NextResponse.json({error:"Sign in required."},{status:401});const {id}=await c.params;if(id===s.user.id)return NextResponse.json({error:"You cannot block yourself."},{status:400});await prisma.userBlock.upsert({where:{blockerId_blockedId:{blockerId:s.user.id,blockedId:id}},update:{},create:{blockerId:s.user.id,blockedId:id}});return NextResponse.json({blocked:true});}
export async function DELETE(_r:Request,c:{params:Promise<{id:string}>}){const s=await auth();if(!s?.user?.id)return NextResponse.json({error:"Sign in required."},{status:401});const {id}=await c.params;await prisma.userBlock.deleteMany({where:{blockerId:s.user.id,blockedId:id}});return NextResponse.json({blocked:false});}
