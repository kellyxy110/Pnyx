import { NextResponse } from "next/server";
import { auth } from "@/auth";
import { prisma } from "@/lib/prisma";
import { generateAi } from "@/lib/ai";
import { saveAiOutput } from "@/lib/ai-persistence";
export async function POST(request: Request) { const session=await auth(); if(!session?.user?.id)return NextResponse.json({error:"Sign in required."},{status:401}); const preference=await prisma.user.findUnique({where:{id:session.user.id},select:{aiEnabled:true}}); if(!preference?.aiEnabled)return NextResponse.json({error:"AI assistance is disabled in your preferences."},{status:403}); const body=await request.json().catch(()=>null) as {postId?:string;replyIds?:string[]} | null; if(!body?.postId)return NextResponse.json({error:"Select a public discussion first."},{status:400}); const post=await prisma.post.findUnique({where:{id:body.postId,isDeleted:false,isDraft:false,visibility:"PUBLIC"},select:{id:true,title:true,body:true,replies:{where:{id:{in:body.replyIds??[]},isDeleted:false},select:{id:true,body:true},orderBy:{createdAt:"asc"}}}}); if(!post)return NextResponse.json({error:"Selected public discussion not found."},{status:404}); const sourceIds=[post.id,...post.replies.map(reply=>reply.id)]; const result=await generateAi("ARTIFACT_DRAFT",`Discussion title: ${post.title}\nDiscussion body: ${post.body}\nSelected replies:\n${post.replies.map(reply=>`[${reply.id}] ${reply.body}`).join("\n")}`); const output=await saveAiOutput({kind:"ARTIFACT_DRAFT",result,requestedById:session.user.id,sourceIds}); return NextResponse.json({output,text:result.text,status:result.status,message:result.error,attribution:{sourcePostId:post.id,replyIds:post.replies.map(reply=>reply.id),note:"AI draft only; review and edit before publishing."}}); }