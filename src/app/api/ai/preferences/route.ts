import { NextResponse } from "next/server";
import { auth } from "@/auth";
import { prisma } from "@/lib/prisma";
export async function GET() { const session = await auth(); if (!session?.user?.id) return NextResponse.json({ error: "Sign in required." }, { status: 401 }); const user = await prisma.user.findUnique({ where: { id: session.user.id }, select: { aiEnabled: true } }); return NextResponse.json({ aiEnabled: user?.aiEnabled ?? false }); }
export async function PATCH(request: Request) { const session = await auth(); if (!session?.user?.id) return NextResponse.json({ error: "Sign in required." }, { status: 401 }); const body = await request.json().catch(() => null) as { aiEnabled?: boolean } | null; if (typeof body?.aiEnabled !== "boolean") return NextResponse.json({ error: "aiEnabled must be boolean." }, { status: 400 }); const user = await prisma.user.update({ where: { id: session.user.id }, data: { aiEnabled: body.aiEnabled }, select: { aiEnabled: true } }); return NextResponse.json(user); }