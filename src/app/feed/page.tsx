import Link from "next/link"; import { auth } from "@/auth"; import { Feed } from "@/components/feed"; import { PostComposer } from "@/components/post-composer";
export default async function FeedPage(){const session=await auth();return <main className="shell space-y-8"><header className="flex flex-wrap items-center justify-between gap-4"><div><p className="eyebrow">Pnyx Commons</p><h1 className="text-4xl font-semibold text-[var(--navy)]">Where conversations become knowledge.</h1></div><nav className="flex gap-3"><Link className="button-outline" href="/explore">Explore</Link><Link className="button-outline" href="/spaces">Spaces</Link><Link className="button-outline" href="/profile">Profile</Link><Link className="button-outline" href="/notifications">Notifications</Link></nav></header>{session?<PostComposer/>:<div className="panel"><p>Sign in to publish and participate.</p><Link className="button-primary mt-4 inline-flex" href="/sign-in?callbackUrl=/feed">Sign in</Link></div>}<Feed/></main>}
