import Link from "next/link";
import { auth } from "@/auth";
import { ProductNav } from "@/components/product-nav";
import { Feed } from "@/components/feed";
import { PostComposer } from "@/components/post-composer";

export default async function FeedPage(){const session=await auth();return <main id="main-content" tabIndex={-1} className="app-shell"><ProductNav/><section className="surface-hero compact"><p className="eyebrow">Community</p><h1>Ask, answer, and build in public.</h1><p>Daily conversations are the starting point. The best contributions become knowledge the whole commons can use.</p></section><section className="community-layout"><div className="community-main">{session?<PostComposer/>:<div className="panel"><h2>Join the conversation</h2><p className="help">Sign in to publish questions, share work, and help someone find a useful answer.</p><Link className="button-primary mt-4 inline-flex" href="/sign-in?callbackUrl=/feed">Sign in to participate</Link></div>}<Feed/></div><aside className="context-rail" aria-label="Community guidance"><div className="panel"><p className="eyebrow">A useful contribution</p><h2>Bring context with you.</h2><p className="help">Name the Space, show what you tried, cite sources, and mark an answer when it solves the problem.</p><Link className="text-link" href="/knowledge">Explore organised knowledge →</Link></div></aside></section></main>}