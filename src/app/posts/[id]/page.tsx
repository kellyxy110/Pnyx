import { notFound } from "next/navigation"; import { prisma } from "@/lib/prisma"; import { PostDetail } from "@/components/post-detail"; import { ProductNav } from "@/components/product-nav";
export default async function PostPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const post=await prisma.post.findUnique({where:{id,isDeleted:false,isDraft:false},include:{author:{select:{displayName:true,username:true}},space:{select:{name:true,slug:true}},replies:{where:{isDeleted:false},orderBy:[{isAccepted:"desc"},{createdAt:"asc"}],include:{author:{select:{displayName:true,username:true}}}}}});if(!post)notFound();return <><ProductNav/><PostDetail post={post}/></>}
